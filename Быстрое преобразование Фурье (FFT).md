---
title: Быстрое преобразование Фурье (FFT)
date: 2026-03-16
tags:
  - математика
  - арифметика
  - FFT
  - быстрое-преобразование-Фурье
  - сложность
aliases:
  - FFT
  - Быстрое преобразование Фурье
  - Кули-Тьюки
---

## Связанные темы
[[Дискретное преобразование Фурье]] | [[Матрица Вандермонда и обратное ДПФ]] | [[Алгоритм Карацубы]] | [[Корни из единицы и примитивный корень]]

---

## Постановка задачи

**Вход:** $n = 2^k$, $\omega \in R$ — примитивный корень $n$-й степени из единицы, $f \in R[x]$, $\deg f < n$.

**Выход:** $\mathrm{DFT}_\omega(f) = \bigl(f(1),\, f(\omega),\, f(\omega^2),\, \ldots,\, f(\omega^{n-1})\bigr)$

---

## Алгоритм FFT (Кули–Тьюки)

> [!algorithm] Алгоритм $\mathrm{FFT}(n,\, \omega,\, f)$
>
> **1. База рекурсии:** если $n = 1$, вернуть $f_0$.
>
> **2. Разбиение:** вычислить два вспомогательных многочлена степени $< n/2$:
>
> $$z_0 := \sum_{j=0}^{n/2 - 1} (f_j + f_{n/2+j})\, x^j$$
>
> $$z_1 := \sum_{j=0}^{n/2 - 1} (f_j - f_{n/2+j})\, x^j \cdot \omega^j$$
>
> **3. Рекурсия:**
>
> $$A \leftarrow \mathrm{FFT}\!\left(\tfrac{n}{2},\, \omega^2,\, z_0\right)$$
> $$B \leftarrow \mathrm{FFT}\!\left(\tfrac{n}{2},\, \omega^2,\, z_1\right)$$
>
> *(заметим: $\omega^2$ — примитивный корень степени $n/2$)*
>
> **4. Сборка результата:** вернуть
> $$\bigl(A_0,\, B_0,\, A_1,\, B_1,\, \ldots,\, A_{n/2-1},\, B_{n/2-1}\bigr)$$
> где $A_i = f(\omega^{2i})$ — значения в чётных точках, $B_i = f(\omega^{2i+1})$ — в нечётных.

> [!note] Смысл разбиения
> $z_0$ кодирует суммы пар коэффициентов (чётная и нечётная половина), $z_1$ — разности, умноженные на соответствующие степени $\omega$. Рекурсивные вызовы вычисляют ДПФ размера $n/2$ с $\omega^2$.

---

## Применение к умножению многочленов

Благодаря [[Дискретное преобразование Фурье#Ключевая лемма свёртка через ДПФ|лемме о свёртке]]:

$$f \cdot g = \mathrm{DFT}_\omega^{-1}\!\bigl(\mathrm{DFT}_\omega(f) \cdot \mathrm{DFT}_\omega(g)\bigr) \qquad \text{если } \deg f + \deg g < n$$

**Схема умножения:**
1. $\hat{f} = \mathrm{FFT}(n, \omega, f)$ — за $O(n \log n)$
2. $\hat{g} = \mathrm{FFT}(n, \omega, g)$ — за $O(n \log n)$
3. $\hat{h} = \hat{f} \cdot \hat{g}$ покоординатно — за $O(n)$
4. $h = \mathrm{FFT}(n, \omega^{-1}, \hat{h}) / n$ — за $O(n \log n)$

---

## Анализ сложности FFT

Пусть в алгоритме FFT:
- $S(n)$ — число **сложений**
- $T(n)$ — число **умножений**

### Рекуррентные соотношения

$$S(n) = 2\,S\!\left(\tfrac{n}{2}\right) + n, \quad S(1) = 0$$

$$T(n) = 2\,T\!\left(\tfrac{n}{2}\right) + \tfrac{n}{2}, \quad T(1) = 0$$

*(На шаге 2 выполняется $n$ сложений/вычитаний и $n/2$ умножений на $\omega^j$)*

### Решение рекуррентностей

По **мастер-теореме** (или прямым раскрытием):

$$S(n) = O(n \log n)$$

$$T(n) = O(n \log n)$$

> [!summary] Сложность FFT
> $$\boxed{S(n) = O(n \log n), \quad T(n) = O(n \log n)}$$
>
> **Итого:** FFT работает за $O(n \log n)$ — что значительно лучше наивного $O(n^2)$ и даже [[Алгоритм Карацубы|Карацубы]] $O(n^{\log_2 3})$.

---

## Итог

FFT — рекурсивный алгоритм вычисления ДПФ за $O(n \log n)$. Ключевая идея: разбить вычисление значений в $n$ точках на два вычисления в $n/2$ точках, используя структуру корней из единицы. В сочетании с леммой о свёртке, FFT даёт оптимальный алгоритм умножения многочленов.
