---
title: Деление Ньютона
date: 2026-03-02
tags:
  - математика
  - арифметика
  - многочлены
  - алгоритмы
aliases:
  - Обращение многочлена
  - k-реверсия
  - Алгоритм обращения Ньютона
---

## Связанные темы
[[Решение скалярных уравнений]] | [[Метод Ньютона для систем уравнений]] | [[Алгоритм деления многочленов]] | [[Кольца многочленов]] | [[Быстрое преобразование Фурье (FFT)]]

---

## Нахождение обратного элемента в кольце

Пусть $R$ — коммутативное кольцо с $1$, $r \in R$, $r$ — обратим. Нужно найти $r^{-1}$.

Рассмотрим уравнение $\frac{1}{x} = r$, т.е. $f(x) = \frac{1}{x} - r = 0$.

Производная: $f'(x) = -\frac{1}{x^2}$.

Применим формулу Ньютона $x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}$:

$$x_{n+1} = x_n - \frac{\frac{1}{x_n} - r}{-\frac{1}{x_n^2}} = x_n + \left(\frac{1 - x_n r}{x_n} \cdot \frac{x_n^2}{1}\right)$$

$$= x_n + x_n - x_n^2 r = 2x_n - x_n^2 r$$

> [!algorithm] Итерация для нахождения обратного
> $$\boxed{x_{n+1} = 2x_n - x_n^2 \cdot r}$$
> Используется для нахождения обратного элемента в кольце многочленов / формальных степенных рядов.

---

## $k$-реверсия (реверсия) многочлена

> [!definition] $k$-реверсия
> Пусть $f \in R[x]$, $k \geq \deg f$.
>
> **$k$-реверсией** (или $k$-обращением) называется:
> $$\mathrm{rev}_k(f) := x^k \cdot f\!\left(\frac{1}{x}\right)$$

**Явная формула:** если $f = \sum_{i=0}^{n} f_i x^i$, то

$$\mathrm{rev}_k(f) = \sum_{j=0}^{k} f_{k-j}\, x^j$$

то есть коэффициенты записываются в обратном порядке (с дополнением нулями до степени $k$).

---

## Связь деления и реверсии

Пусть $m = \deg f \geq n = \deg g$, $f, g \in R[x]$.

Деление с остатком: $f = q \cdot g + r$.

Применяя $\mathrm{rev}_m$:

$$\mathrm{rev}_m(f) = x^m f\!\left(\tfrac{1}{x}\right) = x^m q\!\left(\tfrac{1}{x}\right) g\!\left(\tfrac{1}{x}\right) + x^m r\!\left(\tfrac{1}{x}\right)$$

$$= x^{m-n} q\!\left(\tfrac{1}{x}\right) \cdot \underbrace{x^n g\!\left(\tfrac{1}{x}\right)}_{\mathrm{rev}_n(g)} + \underbrace{x^{m-n+1} \cdot x^{n-1} r\!\left(\tfrac{1}{x}\right)}_{x^{m-n+1}\cdot\mathrm{rev}_{n-1}(r)}$$

$$= \mathrm{rev}_{m-n}(q) \cdot \mathrm{rev}_n(g) + x^{m-n+1} \cdot \mathrm{rev}_{n-1}(r)$$

> [!note] Ключевое наблюдение
> Благодаря модулю $x^{m-n+1}$ можно отбросить остаток от деления $\frac{f(x)}{g(x)}$. В реверсии это означает — отбросить старшие степени при $x$. Так можно вычислить **частное**, не зная остатка:
>
> $$q = \mathrm{rev}_{m-n}\!\left(\mathrm{rev}_m(f) \cdot \left(\mathrm{rev}_n(g)\right)^{-1}\right) \pmod{x^{m-n+1}}$$

Или эквивалентно:

$$\mathrm{rev}_m f \equiv \mathrm{rev}_{m-n}(q) \cdot \mathrm{rev}_n(g) \pmod{x^{m-n+1}}$$

---

## Обращение в кольце формальных степенных рядов

Очевидно, что $R[[x]] \supset R[x]$.

Чтобы $f = \sum_j a_j x^j$ было обратимо в $R[[x]]$, необходимо и достаточно:

$$\left(\sum_j a_j x^j\right)\left(\sum_k b_k x^k\right) = 1$$

Приравнивая коэффициенты:
- $(l = 0)$: $a_0 b_0 = 1$
- $(l = 1)$: $a_0 b_1 + b_0 a_1 = 0$
- $(l = 2)$: $a_0 b_2 + a_1 b_1 + a_2 b_0 = 0$
- и т.д.

Возможно, если $a_0 \in R^*$ (т.е. $a_0$ обратим в $R$).

---

## Лемма об обращении (алгоритм Ньютона для многочленов)

> [!theorem] Лемма
> Пусть $f \in R[[x]]$ и $f(0) = f_0 \in R^*$ (обратим).
>
> Пусть $g_0 = f_0^{-1}$ и $g_{i+1} = 2g_i - f \cdot g_i^2 \pmod{x^{2^{i+1}}}$.
>
> Тогда $f \cdot g_i \equiv 1 \pmod{x^{2^i}}$.

> [!proof]- Доказательство (индукция)
> **База** $i = 0$:
> $$f \cdot g_0 = (f_0 + f_1 x + f_2 x^2 + \ldots) f_0^{-1} = f_0 \cdot f_0^{-1} + f_0^{-1} \cdot f_1 x + \ldots = 1 \pmod{x^{2^0}}$$
>
> **Шаг** $i \to i+1$:
> $$1 - f \cdot g_{i+1} = 1 - 2fg_i + f^2 g_i^2 \pmod{x^{2^{i+1}}}$$
> $$= (1 - fg_i)^2 \pmod{x^{2^{i+1}}}$$
>
> По предположению индукции $1 - fg_i \equiv 0 \pmod{x^{2^i}}$, значит $1 - fg_i = p \cdot x^{2^i}$ для некоторого $p$.
>
> Тогда $(1 - fg_i)^2 = (p\,x^{2^i})^2 \equiv 0 \pmod{x^{2^{i+1}}}$.
>
> Следовательно $1 - fg_{i+1} \equiv 0 \pmod{x^{2^{i+1}}}$. $\blacksquare$

---

## Алгоритм обращения Ньютона

> [!algorithm] Алгоритм обращения Ньютона
> **Вход:** $f \in R[x]$, $f(0)$ обратим, $l \in \mathbb{N}$
>
> **Выход:** $g \in R[x]$ такой, что $f \cdot g \equiv 1 \pmod{x^l}$
>
> **Шаг 1.** $g := (f(0))^{-1}$; $\quad k := \lceil\log_2 l\rceil$
>
> **Шаг 2.** Для $i = 1, \ldots, k$:
> $$g := (2g - f \cdot g^2) \pmod{x^{2^i}}$$
>
> **Шаг 3.** Вернуть $g$

---

## Алгоритм деления Ньютона

> [!algorithm] Алгоритм деления Ньютона
> **Вход:** $f, g \in R[x]$, $g \neq 0$, $\mathrm{lc}(g) \in R^*$
>
> **Выход:** $q, r \in R[x]$ такие, что $f = g \cdot q + r$
>
> **Шаг 1.** Если $\deg f < \deg g$, вернуть $q = 0$, $r = f$.
>
> **Шаг 2.** Положить $m = \deg f - \deg g$.
>
> **Шаг 3.** $h := \text{обращение Ньютона}(\mathrm{rev}_{\deg g}(g),\; m+1)$
>
> **Шаг 4.** $Q := (\mathrm{rev}_{\deg f}(f) \cdot h) \pmod{x^{m+1}}$
>
> **Шаг 5.** Вернуть $q = \mathrm{rev}_m Q$, $\quad r = f - g \cdot q$

---

## Сложность алгоритма

Пусть $M(l)$ — сложность умножения двух многочленов степени $< l$.

Для $l = 2^k$ требуется $3M(l) + 1$ операций в кольце коэффициентов.

> [!summary] Итог
> Деление Ньютона сводит задачу нахождения частного при делении многочленов к умножению через операцию реверсии и обращения в кольце формальных степенных рядов. Сложность — $O(M(n))$, что оптимально при использовании [[Быстрое преобразование Фурье (FFT)|FFT]] для умножения.
