---
title: Алгоритм Карацубы
date: 2026-03-09
tags:
  - математика
  - арифметика
  - алгоритмы
  - умножение
  - карацуба
  - сложность
aliases:
  - Умножение Карацубы
  - Karatsuba
---

## Связанные темы
[[Кольцо многочленов от n переменных и идеалы]] | [[Алгоритм деления многочленов]] | [[Двоичные многочлены и поле GF(2^n)]]

---

## Мотивация: линейный случай

Рассмотрим два многочлена степени 1:

$$f = f_0 + f_1 x, \qquad g = g_0 + g_1 x$$

Наивно:

$$f \cdot g = f_0 g_0 + (f_0 g_1 + f_1 g_0)\, x + f_1 g_1\, x^2$$

Это **4 умножения**. Идея Карацубы: вычислить средний коэффициент хитрее.

> [!algorithm] Трюк Карацубы (степень 1)
> Вычислим три вспомогательных произведения:
> $$A = f_0 \cdot g_0, \quad B = f_1 \cdot g_1, \quad C = (f_0 + f_1)(g_0 + g_1)$$
>
> Тогда:
> $$f \cdot g = A + (C - A - B)\,x + B\,x^2$$
>
> **Итого: 3 умножения** вместо 4 (сложения дёшевы).

---

## Рекурсивный алгоритм Карацубы

Пусть $n = 2^k$, и $f, g \in R[x]$ с $\deg f < n$, $\deg g < n$.

### Шаг 1: Разбиение

Пусть $m = n/2 = 2^{k-1}$. Разобьём $f$ на «младшую» и «старшую» части:

$$f = \sum_{j=0}^{n-1} f_j x^j = \underbrace{(f_0 + f_1 x + \cdots + f_{m-1} x^{m-1})}_{F_0} + x^m \cdot \underbrace{(f_m + f_{m+1} x + \cdots + f_{2m-1} x^{m-1})}_{F_1}$$

$$f = F_0 + x^m F_1$$

Аналогично: $g = G_0 + x^m G_1$, где $F_0, F_1, G_0, G_1 \in R[x]$, $\deg F_0, \deg F_1, \deg G_0, \deg G_1 < m$.

### Шаг 2: Применение Карацубы

$$f \cdot g = (F_0 + x^m F_1)(G_0 + x^m G_1)$$

По аналогии с линейным случаем:

$$\boxed{f \cdot g = F_0 G_0 + \bigl(U - F_0 G_0 - F_1 G_1\bigr)\, x^m + F_1 G_1\, x^{2m}}$$

где

$$U = (F_0 + F_1)(G_0 + G_1)$$

> [!note] Три рекурсивных умножения
> - $F_0 G_0$ — рекурсивно
> - $F_1 G_1$ — рекурсивно
> - $U = (F_0 + F_1)(G_0 + G_1)$ — рекурсивно
>
> Умножение на $x^m$ и $x^{2m}$ — это просто **сдвиг** массива коэффициентов, стоит $O(n)$.

### Критерий остановки рекурсии

Рекурсия останавливается при $n = 1$ (умножение скаляров).

> [!warning] Нечётная степень
> Если степень многочлена нечётная, **дополняем нулевыми коэффициентами** до ближайшей степени двойки перед применением алгоритма.

---

## Анализ сложности

Пусть $T(n)$ — стоимость умножения двух многочленов степени $< n$ алгоритмом Карацубы.

### Рекуррентное соотношение

$$T(n) = 3 \cdot T\!\left(\frac{n}{2}\right) + O(n), \qquad T(1) = 1$$

*(три рекурсивных вызова для половинного размера + линейная работа по сложению/сдвигу)*

### Раскрытие рекуррентности

Пусть $n = 2^k$. Раскроем:

$$T(2^k) = 3\,T(2^{k-1}) + 2^{k+2}$$
$$= 3\!\left(3\,T(2^{k-2}) + 2^{k+1}\right) + 2^{k+2}$$
$$= 3^2\,T(2^{k-2}) + 3 \cdot 2^{k+1} + 2^{k+2}$$
$$= 3^k \cdot T(1) + 2^{k+2}\!\left(1 + \frac{3}{2} + \frac{9}{4} + \frac{27}{8} + \cdots\right)$$

Геометрическая прогрессия со знаменателем $3/2$:

$$\sum_{i=0}^{k-1} \left(\frac{3}{2}\right)^i = \frac{1 - (3/2)^k}{1 - 3/2} = 2\!\left(\!\left(\frac{3}{2}\right)^{\!k} - 1\right)$$

Итого:

$$T(2^k) = 3^k + 2^{k+2} \cdot \frac{1 - (3/2)^k}{1 - 3/2} = O(3^k)$$

### Перевод обратно через $n$

Из $n = 2^k$ следует $k = \log_2 n$, поэтому:

$$3^k = 3^{\log_2 n} = n^{\log_2 3}$$

> [!summary] Асимптотика алгоритма Карацубы
> $$\boxed{T(n) = O\!\left(n^{\log_2 3}\right) \approx O(n^{1.585})}$$
>
> Это лучше наивного $O(n^2)$.

---

## Итог

Алгоритм Карацубы — классический пример техники «разделяй и властвуй». Ключевая идея: сократить число рекурсивных умножений с 4 до 3 за счёт дополнительных сложений. Это даёт асимптотику $O(n^{\log_2 3}) \approx O(n^{1.585})$ против наивных $O(n^2)$.

