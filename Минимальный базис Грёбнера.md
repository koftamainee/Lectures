---
title: Минимальный базис Грёбнера
date: 2026-04-13
tags:
  - математика
  - арифметика
  - базис-Грёбнера
aliases:
  - Редуцированный базис Грёбнера
  - Минимальный базис
---

## Связанные темы
[[Базис Грёбнера]] | [[Алгоритм Бухбергера]] | [[Критерий Бухбергера]] | [[S-полином]]

---

## Определение

> [!definition] Определение: Минимальный базис Грёбнера
> Конечное множество $G = \{g_1, \ldots, g_s\} \subset I$ называется **минимальным базисом Грёбнера** идеала $I$, если $G$ — базис Грёбнера и:
> 1. $lc(p) = 1$ для всех $p \in G$
> 2. $LT(p) \notin \langle LT(G \setminus \{p\}) \rangle$ для всех $p \in G$

---

## Построение: развёрнутый пример

Рассмотрим $I = \langle f_1, f_2 \rangle \subset \mathbb{Q}[x,y]$ с упорядочиванием $>_{\mathrm{grlex}}$:
$$f_1 = x^2 - 2xy, \qquad f_2 = x^2y - 2y^2 + x$$

### Итерация 1

Вычислим $S(f_1, f_2)$. Здесь $\mathrm{lcm}(x^2, x^2y) = x^2y$, поэтому:
$$S(f_1, f_2) = \frac{x^2y}{x^2} f_1 - \frac{x^2y}{x^2y} f_2 = y f_1 - f_2 = -x^2$$

Редуцируем $-x^2$ на $\{f_1, f_2\}$: ни $x^2$ (из $f_1$), ни $x^2y$ (из $f_2$) не делят $-x^2$... нет,
$LT(f_1) = x^2$ делит $LT(-x^2) = x^2$, поэтому $-x^2 \xrightarrow{f_1} 0 + $ остаток. Вычислим:
$$-x^2 - \frac{-x^2}{x^2} f_1 = -x^2 + f_1 = -x^2 + x^2 - 2xy = -2xy$$

Далее $-2xy$: $LT(f_1) = x^2$ не делит $x^2 y^1$... нет, $x^2$ не делит $xy$. $LT(f_2) = x^2y$ не делит $xy$. Остаток $r = -2xy \neq 0$. Полагаем $f_3 := xy$ (нормируем).

### Итерация 2

$F = \{f_1, f_2, f_3\}$, $f_3 = xy$:

- $S(f_1, f_2)^F = 0$ (уже обработан)
- $S(f_1, f_3) = \frac{x^2y}{x^2} f_1 - \frac{x^2y}{xy} f_3 = yf_1 - xf_3 = y(x^2-2xy) - x(xy) = x^2y - 2xy^2 - x^2y = -2xy^2$

  Редуцируем $-2xy^2$ на $F$: $LT(f_3) = xy$ делит $xy^2$, получаем:
  $$-2xy^2 + 2y \cdot f_3 = -2xy^2 + 2xy^2 = 0 \checkmark$$

- $S(f_2, f_3) = \frac{x^2y}{x^2y} f_2 - \frac{x^2y}{xy} f_3 = f_2 - xf_3 = x^2y - 2y^2 + x - x^2y = -2y^2 + x$

  Редуцируем $-2y^2 + x$ на $F$: ни $x^2$, ни $x^2y$, ни $xy$ не делят $y^2$. Остаток $r = -2y^2 + x \neq 0$.
  
  Полагаем $f_4 := y^2 - \tfrac{1}{2}x$ (нормируем).

### Итерация 3

$F = \{f_1, f_2, f_3, f_4\}$:

Проверяем все пары с участием $f_4$:
- $S(f_1, f_4)^F = 0$
- $S(f_2, f_4)^F = 0$
- $S(f_3, f_4) = \frac{xy^2}{xy} f_3 - \frac{xy^2}{y^2} f_4 = yf_3 - xf_4 = xy^2 - x(y^2 - \tfrac{1}{2}x) = \tfrac{1}{2}x^2$

  Редуцируем $\tfrac{1}{2}x^2$ на $F$: $LT(f_1) = x^2$ делит $x^2$, получаем:
  $$\tfrac{1}{2}x^2 - \tfrac{1}{2}f_1 = \tfrac{1}{2}x^2 - \tfrac{1}{2}(x^2 - 2xy) = xy$$
  Далее $LT(f_3) = xy$ делит $xy$: $xy - f_3 = xy - xy = 0 \checkmark$

Все $S$-полиномы редуцируются в 0. Алгоритм завершён:
$$G = \{f_1, f_2, f_3, f_4\} = \{x^2 - 2xy,\; x^2y - 2y^2 + x,\; xy,\; y^2 - \tfrac{1}{2}x\}$$

### Построение минимального базиса

Проверяем, нет ли лишних элементов:

- $LT(f_2) = x^2y$: делится ли на $LT(f_3) = xy$? Да: $x^2y / xy = x$. Значит $f_2$ **избыточен**.

Убираем $f_2$. Итоговый минимальный базис Грёбнера:
$$G_{\min} = \{x^2 - 2xy,\; xy,\; y^2 - \tfrac{1}{2}x\}$$

---

## Лемма об удалении элемента

> [!theorem] Лемма
> Пусть $G$ — базис Грёбнера идеала $I$, $p \in G$ и $LT(p) \in \langle LT(G \setminus \{p\}) \rangle$.
> Тогда $G \setminus \{p\}$ тоже является базисом Грёбнера идеала $I$.

---

## Редуцированный базис Грёбнера

> [!definition] Определение: Редуцированный базис Грёбнера
> Минимальный базис Грёбнера $G$ называется **редуцированным**, если дополнительно для каждого $p \in G$ ни один моном в $p$ (кроме $LT(p)$) не делится на $LT(q)$ для любого $q \in G \setminus \{p\}$.

> [!theorem] Единственность
> Для каждого ненулевого идеала $I$ и фиксированного мономиального упорядочивания редуцированный базис Грёбнера **единственен**.

---

## Итог

> [!summary]
> Минимальный (редуцированный) базис Грёбнера — каноническая форма идеала при фиксированном упорядочивании. Строится из результата алгоритма Бухбергера двумя шагами: удаление избыточных элементов (лемма), затем редукция хвостов.
