---
title: Алгоритм RSA
date: 2026-02-18
tags:
  - криптография
  - информационная-безопасность
  - асимметричное-шифрование
aliases:
  - RSA
  - Шифрование RSA
---

## Связанные темы
[[Функция Эйлера и теорема Эйлера]] | [[Основы криптографии]] | [[Трудные задачи криптографии]]

---

## Алгоритм RSA

> [!algorithm] Генерация ключей
> **Шаг 1.** Выбрать два простых числа $p, q$. Вычислить:
> $$N = p \cdot q \quad \text{($N$ — публичен, $p, q$ — секрет)}$$
>
> **Шаг 2.** Выбрать **шифрующую экспоненту** $e$:
> $$\gcd(e,\, \varphi(N)) = 1$$
>
> **Шаг 3.** Пара $(e, N)$ — **публичный ключ**.

> [!algorithm] Шифрование
> **Шаг 4.** Сообщение $m$ — число в системе вычетов $N$ (т.е. $m \in \mathbb{Z}_N$).
>
> $$\boxed{C = m^e \bmod N}$$

> [!algorithm] Нахождение дешифрующей экспоненты
> **Шаг 5.** Найти $d$ — **дешифрующую экспоненту**:
> $$d \cdot e \equiv 1 \pmod{\varphi(N)}$$
>
> $d$ находится через **расширенный алгоритм Евклида**:
> $$(x, y) \mapsto (a, b, g), \quad g = \gcd(x, y), \quad ax + by = g$$
>
> Применяем к паре $(e,\, \varphi(N))$:
> $$(e,\, \varphi(N)) \mapsto (a, b, 1) \Rightarrow a \cdot e + b \cdot \varphi(N) = 1$$
>
> Значит $d = a \bmod \varphi(N)$.

> [!algorithm] Дешифрование
> **Шаг 6.**
> $$\boxed{m = C^d \bmod N}$$

---

## Корректность RSA

> [!theorem] Корректность (через теорему Эйлера)
> $D_k(E_k(m)) = m$, т.е. дешифрование обратно шифрованию.

> [!proof]- Доказательство
> $$C^d \bmod N = (m^e)^d \bmod N = m^{ed} \bmod N$$
>
> Из условия: $e \cdot d \equiv 1 \pmod{\varphi(N)}$, значит $e \cdot d = k\,\varphi(N) + 1$ для некоторого $k \in \mathbb{Z}$.
>
> $$m^{ed} = m^{k\varphi(N)+1} = m^{k\varphi(N)} \cdot m = \left(m^{\varphi(N)}\right)^k \cdot m$$
>
> По [[Функция Эйлера и теорема Эйлера|теореме Эйлера]]: $m^{\varphi(N)} \equiv 1 \pmod{N}$.
>
> $$m^{k\varphi(N)} \cdot m \equiv 1^k \cdot m = m \pmod{N} \quad \blacksquare$$
