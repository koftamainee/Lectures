---
title: Китайская теорема об остатках
date: 2026-04-02
tags:
  - криптография
  - теория-чисел
  - информационная-безопасность
aliases:
  - КТО
  - CRT
  - Chinese Remainder Theorem
---

## Связанные темы
[[Функция Эйлера и теорема Эйлера]] | [[Алгоритм RSA]]

---

## Постановка задачи

Дано $b_1, b_2, \ldots, b_k$ и попарно взаимно простые $m_1, m_2, \ldots, m_k$:
$$\forall i \neq j : \gcd(m_i, m_j) = 1$$

Решить систему:
$$(*)\quad \begin{cases}
x \equiv b_1 \pmod{m_1} \\
x \equiv b_2 \pmod{m_2} \\
\quad\vdots \\
x \equiv b_k \pmod{m_k}
\end{cases}$$

> [!note] Частный случай $k = 1$
> $x \equiv b_1 \pmod{m_1}$, т.е. $x = b_1 + m_1 \cdot q$, $q \in \mathbb{Z}$.

---

## Построение решения

Введём обозначения. Для $s \in \{1, \ldots, k\}$:

$$M_s := \prod_{j \neq s} m_j = \frac{\prod_{j=1}^k m_j}{m_s} \quad \text{(произведение всех, кроме $m_s$)}$$

> [!definition] Мультипликативное обратное $M_s'$
> $$M_s \cdot M_s' \equiv 1 \pmod{m_s}$$
> (существует, так как $M_s$ взаимно просто с $m_s$)
> $$M_s' = M_s^{-1} \bmod m_s$$

**Частное решение:**

$$x_0 := M_1 M_1' b_1 + M_2 M_2' b_2 + \cdots + M_k M_k' b_k = \sum_{j=1}^k M_j M_j' b_j$$

(операции не из кольца — вычисляются в $\mathbb{Z}$)

**Общее решение:**

$$x = x_0 + q \cdot \prod_{j=1}^k m_j, \quad q \in \mathbb{Z}$$

Или, эквивалентно: $x \equiv x_0 \pmod{\prod_{j=1}^k m_j}$.

---

## Теорема

> [!theorem] Китайская теорема об остатках
> При условии попарной взаимной простоты модулей система $(*)$ имеет **единственное** решение по модулю $\prod_{j=1}^k m_j$.
>
> Это решение: $x \equiv x_0 \pmod{\prod m_j}$, где $x_0 = \sum_{j=1}^k M_j M_j' b_j$.

> [!proof]- Проверка корректности (КТО)
> Проверим, что $x_0$ удовлетворяет $s$-му уравнению системы:
>
> $$\left(\sum_{j=1}^k M_j M_j' b_j + q \prod_{j=1}^k m_j\right) \bmod m_s = b_s$$
>
> При $j \neq s$: слагаемое $M_j M_j' b_j$ содержит множитель $m_s$ (т.к. $m_s \mid M_j$), значит $\equiv 0 \pmod{m_s}$.
>
> При $j = s$: $M_s M_s' b_s \equiv 1 \cdot b_s = b_s \pmod{m_s}$.
>
> Слагаемое $q \prod m_j$ также $\equiv 0 \pmod{m_s}$.
>
> Итого: $x_0 \equiv b_s \pmod{m_s}$. $\blacksquare$
