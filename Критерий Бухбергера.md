---
title: Критерий Бухбергера
date: 2026-04-06
tags:
  - математика
  - арифметика
  - базис-Грёбнера
aliases:
  - Buchberger criterion
  - критерий Бухбергера S-пар
---

## Связанные темы
[[Базис Грёбнера]] | [[S-полином]] | [[Алгоритм деления многочленов]] | [[Теорема Гилберта о базисе]] | [[Мономиальные идеалы]]

---

## Алгоритм деления с помощью базиса Грёбнера

> [!theorem] Алгоритм деления на базис Грёбнера
> Пусть $G = \{g_1, g_2, \ldots, g_s\}$ — базис Грёбнера идеала $I \subseteq k[x_1, \ldots, x_n]$, и $f \in I$.
>
> Тогда $\exists!\, r \in k[x_1, \ldots, x_n]$ такой, что:
> 1. ни один член $r$ не делится ни на один из $\mathrm{lt}(g_1), \mathrm{lt}(g_2), \ldots, \mathrm{lt}(g_s)$
> 2. $\exists g \in I : \quad f = g + r$

> [!proof]- Доказательство (очевидно)
> Применяем алгоритм деления из [[Алгоритм деления многочленов]] к делению $f$ на $g_1, \ldots, g_s$.
> Существование $r$ — из алгоритма деления. Единственность — из свойства базиса Грёбнера: если был бы другой остаток $r'$, то $r - r' \in I$ имел бы старший член из $\langle LT(I) \rangle$, значит делился бы на какой-то $\mathrm{lt}(g_i)$ — противоречие.

---

## Лемма о представлении (ключевая лемма)

> [!theorem] Лемма о представлении
> Пусть $f_1, \ldots, f_s$ — многочлены от $n$ переменных, $f_i \in k[x_1, \ldots, x_n]$, где $i = 1, 2, \ldots, s$ и $c_i \in k$.
>
> Пусть далее $F := \sum_{i=1}^{s} c_i f_i$, и мультистепень $\mathrm{multideg}(f_i) = \delta \in \mathbb{Z}^n_{\geq 0}$.
>
> Если $\mathrm{multideg}(F) < \delta$, то $F$ является линейной комбинацией S-полиномов с коэффициентами из $k$.
>
> Причём множество S-полиномов определяется как $S(f_i, f_j)$, где $1 \leq i < j \leq s$ и $\mathrm{multideg}(S(f_i, f_j)) < \delta$.

> [!proof]- Доказательство леммы
> **Шаг 1.** Нормируем многочлены. Определим
> $$p_i := \frac{1}{\mathrm{lc}(f_i)} f_i$$
> тогда $\mathrm{lc}(p_i) = 1$ (унитарные многочлены). Запишем:
> $$F = \sum_{i=1}^{s} c_i f_i = \sum_{i=1}^{s} c_i \cdot \mathrm{lc}(f_i) \cdot p_i$$
>
> Заметим, что $\sum_{i=1}^{s} c_i \cdot \mathrm{lc}(f_i) = 0$ (так как $\mathrm{multideg}(F) < \delta$, старшие члены сокращаются).
>
> **Шаг 2.** Выразим через телескопическую сумму. Используя $\sum c_i \mathrm{lc}(f_i) = 0$:
> $$F = \sum_{j=1}^{s} \left(\sum_{\beta=1}^{j} c_\beta \mathrm{lc}(f_\beta)\right)(p_j - p_{j+1})$$
>
> **Шаг 3.** Связь с S-полиномами. Заметим, что при $\mathrm{multideg}(f_i) = \mathrm{multideg}(f_j) = \delta$:
> $$S(f_i, f_j) = \frac{x^\delta}{\mathrm{lt}(f_i)} f_i - \frac{x^\delta}{\mathrm{lt}(f_j)} f_j = p_i - p_j$$
>
> Поскольку $x^\delta / \mathrm{lt}(f_i) = 1/\mathrm{lc}(f_i)$ при $\mathrm{multideg}(f_i) = \delta$.
>
> Таким образом, $F$ является линейной комбинацией $S(f_i, f_j)$ с $\mathrm{multideg}(S(f_i, f_j)) < \delta$.
>
> **Шаг 4.** Для общего случая (разные мультистепени): разбиваем $F = \sum_{m(i)=\delta} + \sum_{m(i)<\delta}$ и применяем те же рассуждения к первой части.
>
> Далее $S(x^{\delta(i)} g_i, x^{\delta(j)} g_j) = \ldots$ (посчитать) — линейная комбинация, мультистепень которой $< \delta$.

---

## Теорема Бухбергера (критерий S-пар)

> [!theorem] Теорема Бухбергера (критерий базиса Грёбнера)
> Пусть $I \subseteq k[x_1, \ldots, x_n]$ — идеал, $G = \{g_1, \ldots, g_s\}$ — базис идеала $I$.
>
> Тогда $G$ является **базисом Грёбнера** $\Longleftrightarrow$ для всех $i \neq j$ остаток от деления $S(g_i, g_j)$ на $G$ в любом порядке равен нулю:
> $$\forall i \neq j : \quad \overline{S(g_i, g_j)}^G = 0$$

> [!proof]- Доказательство
> **($\Rightarrow$) Пусть $G$ — базис Грёбнера идеала $I$.**
>
> Тогда $S(g_i, g_j) \in I$, т.к. $g_i, g_j \in I$ и $I$ — идеал. По свойству деления на базис Грёбнера остаток от деления любого $f \in I$ на $G$ равен $0$. Следовательно, $\overline{S(g_i, g_j)}^G = 0$.
>
> **($\Leftarrow$) Пусть $\overline{S(g_i, g_j)}^G = 0$ для всех $i \neq j$.**
>
> Нужно показать, что $G$ — базис Грёбнера, т.е. $\langle \mathrm{lt}(g_1), \ldots, \mathrm{lt}(g_s) \rangle = \langle LT(I) \rangle$.
>
> Пусть $f \in I$, $f \neq 0$. Запишем $f = \sum_{i=1}^{s} h_i g_i$.
>
> Пусть $m(i) = \mathrm{multideg}(h_i g_i)$ и $\delta = \max_i m(i)$.
>
> Заметим, что $\mathrm{multideg}(f) \leq \delta$.
>
> Рассмотрим все способы получить представление $f$ в виде $\sum h_i g_i$.
> На многочленах задано мономиальное упорядочивание (которое является вполне упорядочиванием) и можно выбрать такое представление $f$, что $\delta$ будет минимальной.
>
> Допустим, что $\mathrm{multideg}(f) < \delta$. Тогда по лемме о представлении:
> $$f = \sum_{\substack{i \\ m(i)=\delta}} \mathrm{lt}(h_i) g_i + \text{(члены меньшей мультистепени)}$$
>
> является линейной комбинацией S-полиномов $S(f_i, f_j)$ с мультистепенями $< \delta$.
>
> Далее, поскольку $\overline{S(g_i, g_j)}^G = 0$, то $S(g_i, g_j) = \sum_k a_{ijk} g_k$, где $\mathrm{multideg}(a_{ijk} g_k) \leq \mathrm{multideg}(S(g_i, g_j)) < \delta$.
>
> Подставляя это в представление $f$, получаем новое представление $f = \sum \tilde{h}_i g_i$, в котором $\tilde{\delta} < \delta$ — противоречие с минимальностью $\delta$.
>
> Значит $\mathrm{multideg}(f) = \delta$, и $\mathrm{lt}(f)$ делится на $\mathrm{lt}(g_i)$ для некоторого $i$.
>
> Таким образом $\langle LT(I) \rangle \subseteq \langle \mathrm{lt}(g_1), \ldots, \mathrm{lt}(g_s) \rangle$, а обратное включение очевидно (все $g_i \in I$).

---

## Итог

> [!summary] Критерий Бухбергера — практическое значение
> Критерий Бухбергера даёт **алгоритмически проверяемое** условие того, является ли данный набор $G$ базисом Грёбнера: достаточно для каждой пары $(g_i, g_j)$ вычислить $S(g_i, g_j)$ и разделить на $G$. Если все остатки нулевые — $G$ является базисом Грёбнера. Это лежит в основе **алгоритма Бухбергера** построения базиса Грёбнера.
