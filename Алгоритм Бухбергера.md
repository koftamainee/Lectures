---
title: Алгоритм Бухбергера
date: 2026-04-13
tags:
  - математика
  - арифметика
  - базис-Грёбнера
  - алгоритм
aliases:
  - Buchberger algorithm
---

## Связанные темы
[[Базис Грёбнера]] | [[S-полином]] | [[Критерий Бухбергера]] | [[Теорема Гилберта о базисе]] | [[Минимальный базис Грёбнера]]

---

## Постановка

> [!definition] Нотация: редукция
> Для $f \in k[x_1, \ldots, x_n]$ и конечного множества $G$ обозначим через $\overline{f}^G$ остаток от деления $f$ на $G$ (в произвольном порядке).

---

## Алгоритм

> [!algorithm] Алгоритм Бухбергера
> **Вход:** $F = \{f_1, \ldots, f_s\}$ — порождающее множество идеала $I \subseteq k[x_1, \ldots, x_n]$.
> **Выход:** базис Грёбнера $G$ идеала $I$.
>
> 1. $G := F$
> 2. **Повторять:**
>    - $G' := G$
>    - Для каждой пары $(p, q) \in G' \times G'$, $p \neq q$:
>      - $S := \overline{S(p,q)}^{G'}$
>      - Если $S \neq 0$, то $G := G \cup \{S\}$
> 3. **Пока** $G \neq G'$
> 4. **Вернуть** $G$

---

## Теорема (корректность и завершимость)

> [!theorem] Теорема об алгоритме Бухбергера
> Пусть $I = \langle f_1, \ldots, f_s \rangle$. Тогда алгоритм Бухбергера завершается за конечное число шагов и возвращает базис Грёбнера идеала $I$.

### Завершимость

> [!proof]- Доказательство завершимости
> Каждый раз, когда $G' \neq G$, мы добавляем $r = \overline{S(p,q)}^{G'} \neq 0$.
>
> По построению $LT(r) \notin \langle LT(G') \rangle$ (иначе $r$ обнулился бы при делении). Следовательно:
> $$\langle LT(G') \rangle \subsetneq \langle LT(G) \rangle$$
>
> Таким образом на каждой итерации идеал $\langle LT(G) \rangle$ строго возрастает. По [[Теорема Гилберта о базисе|теореме об обрыве возрастающих цепей идеалов]] такая цепь обрывается — алгоритм завершается.

### Корректность

> [!proof]- Доказательство корректности
> В конце алгоритма $G' = G$, то есть для всех пар $p, q \in G$, $p \neq q$:
> $$\overline{S(p,q)}^{G} = 0$$
>
> По [[Критерий Бухбергера|критерию Бухбергера]] это означает, что $G$ — базис Грёбнера.
>
> Осталось проверить, что $\langle G \rangle = I$:
> - $G \subset I$ (на каждом шаге добавляется $r = S(p,q) - \sum h_i g_i \in I$), поэтому $\langle G \rangle \subseteq I$.
> - $F \subseteq G$, поэтому $I = \langle F \rangle \subseteq \langle G \rangle$.

---

## Лемма о редукции базиса

> [!theorem] Лемма
> Пусть $G$ — базис Грёбнера идеала $I$, $p \in G$ и $LT(p) \in \langle LT(G \setminus \{p\}) \rangle$.
> Тогда $G \setminus \{p\}$ тоже является базисом Грёбнера идеала $I$.

Эта лемма позволяет итеративно убирать «лишние» элементы и получать [[Минимальный базис Грёбнера]].

---

## Итог

> [!summary]
> Алгоритм Бухбергера — конструктивная процедура получения базиса Грёбнера из любого конечного порождающего множества идеала. Завершимость гарантируется нётеровостью $k[x_1,\ldots,x_n]$, корректность — критерием S-пар.
