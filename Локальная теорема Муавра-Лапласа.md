---
title: "Локальная теорема Муавра-Лапласа"
date: 2026-04-09
tags: [математика, теорвер, муавр-лаплас, предельные-теоремы, нормальное-распределение]
aliases: ["локальная ЦПТ", "теорема Муавра-Лапласа"]
---

## Связанные темы
[[Нормальное распределение]] | [[Дискретные распределения]] | [[Вероятностная мера]]

---

## Постановка задачи

Проводится серия из $n$ испытаний Бернулли, в каждом из которых:
- событие $A$ наступает с вероятностью $p$,
- не наступает с вероятностью $q = 1 - p$.

Обозначим $P_n(k)$ — вероятность того, что событие $A$ наступит ровно $k$ раз в серии из $n$ испытаний.

---

## Формулировка

> [!theorem] Локальная теорема Муавра-Лапласа
> Пусть проводится серия из $n$ испытаний Бернулли. Тогда при $n \to \infty$, если величина
> $$x = \frac{k - np}{\sqrt{npq}}$$
> находится в некотором **конечном** интервале, выполняется:
> $$\lim_{n \to \infty} \frac{P_n(k)}{\dfrac{1}{\sqrt{2\pi npq}}\,e^{-\frac{x^2}{2}}} = 1$$
>
> То есть асимптотически:
> $$\boxed{P_n(k) \approx \frac{1}{\sqrt{2\pi npq}}\,e^{-\frac{x^2}{2}}}, \quad x = \frac{k - np}{\sqrt{npq}}$$

> [!tip] Смысл теоремы
> Биномиальные вероятности в центральной зоне ($x$ конечно) аппроксимируются значениями плотности стандартного нормального распределения. Чем больше $n$, тем точнее приближение.

---

## Доказательство

> [!proof]- Доказательство (через формулу Стирлинга)
>
> **Шаг 1. Явная формула Бернулли.**
>
> $$P_n(k) = \binom{n}{k} p^k q^{n-k} = \frac{n!}{k!\,(n-k)!}\,p^k q^{n-k}$$
>
> **Шаг 2. Формула Стирлинга для $n!$, $k!$, $(n-k)!$.**
>
> Применим $m! \approx \sqrt{2\pi m}\left(\dfrac{m}{e}\right)^m$:
>
> $$P_n(k) = \sqrt{\frac{n}{2\pi k(n-k)}} \cdot \frac{n^n\, e^k\, e^{n-k}}{k^k\,(n-k)^{n-k}\, e^n} \cdot p^k q^{n-k} \cdot e^{O(n^{-1})}$$
>
> После сокращения $e$-членов:
>
> $$= \sqrt{\frac{n}{2\pi k(n-k)}} \cdot \left(\frac{np}{k}\right)^k \cdot \left(\frac{nq}{n-k}\right)^{n-k} \cdot e^{O(n^{-1/2})}$$
>
> Обозначим $A_n = \left(\dfrac{np}{k}\right)^k \cdot \left(\dfrac{nq}{n-k}\right)^{n-k}$.
>
> **Шаг 3. Оценка $\ln A_n$.**
>
> Используем замену $k = np + x\sqrt{npq}$, $n - k = nq - x\sqrt{npq}$:
>
> $$\ln A_n = k\ln\frac{np}{k} + (n-k)\ln\frac{nq}{n-k}$$
>
> $$= (np + x\sqrt{npq})\ln\frac{np}{np + x\sqrt{npq}} + (nq - x\sqrt{npq})\ln\frac{nq}{nq - x\sqrt{npq}}$$
>
> $$= -(np + x\sqrt{npq})\ln\!\left(1 + x\sqrt{\frac{q}{np}}\right) - (nq - x\sqrt{npq})\ln\!\left(1 - x\sqrt{\frac{p}{nq}}\right)$$
>
> Раскладываем логарифм: $\ln(1+t) = t - \dfrac{t^2}{2} + \dfrac{t^3}{3} - \ldots$
>
> После подстановки и алгебраических упрощений (члены первого порядка по $x$ взаимно уничтожаются, члены $O(n^{-1/2})$ стягиваются к нулю):
>
> $$\ln A_n = -\frac{1}{2}x^2 + O(n^{-1/2})$$
>
> Следовательно:
> $$A_n = e^{-\frac{1}{2}x^2} \cdot e^{O(n^{-1/2})}$$
>
> **Шаг 4. Корневой множитель.**
>
> $$\sqrt{\frac{n}{2\pi k(n-k)}} = \sqrt{\frac{n}{2\pi(np+x\sqrt{npq})(nq - x\sqrt{npq})}} \xrightarrow{n\to\infty} \frac{1}{\sqrt{2\pi npq}}$$
>
> **Шаг 5. Итог.**
>
> $$P_n(k) \approx \frac{1}{\sqrt{2\pi npq}}\,e^{-\frac{x^2}{2}} \cdot e^{O(n^{-1/2})} \xrightarrow{n\to\infty} \frac{1}{\sqrt{2\pi npq}}\,e^{-\frac{x^2}{2}} \quad \checkmark$$

---

## Итог

Локальная теорема Муавра-Лапласа показывает, что биномиальное распределение при больших $n$ в центральной зоне ведёт себя как нормальное. Доказательство опирается на формулу Стирлинга и разложение логарифма в ряд.
