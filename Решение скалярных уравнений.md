---
title: Решение скалярных уравнений
date: 2026-03-02
tags:
  - математика
  - арифметика
  - численные-методы
  - корни-уравнений
aliases:
  - Метод дихотомии
  - Метод простой итерации
  - Метод касательных
---

## Связанные темы
[[Метод Ньютона для систем уравнений]] | [[Деление Ньютона]] | [[Алгоритм Карацубы]]

---

## Постановка задачи

Найти корень уравнения $f(x) = 0$, где $f : M \to \mathbb{R}$, $M \subseteq \mathbb{R}$.

---

## 1. Метод дихотомии (бисекции)

> [!definition] Условие применимости
> $f \in C[a, b]$ и $f(a) \cdot f(b) < 0$ (значения разных знаков на концах отрезка).

**Идея:** делить отрезок пополам и выбирать ту половину, на концах которой функция меняет знак.

**Итерационная формула:**

$$c = \frac{a + b}{2}$$

На каждом шаге: $[a_{n+1}, b_{n+1}] \subset [a_n, b_n]$, длина

$$l_n = \frac{b - a}{2^n}$$

**Критерий остановки:** $l_n < \varepsilon$, тогда приближение к корню:

$$x^* \approx \frac{a_n + b_n}{2}$$

> [!note] Сходимость
> Метод сходится всегда при выполнении условия применимости. Сходимость линейная — каждый шаг уменьшает погрешность вдвое.

---

## 2. Метод простой итерации

**Идея:** преобразовать уравнение $f(x) = 0$ к виду $x = \psi(x)$.

Вводим вспомогательную функцию $\lambda(x)$ (произвольную) и записываем:

$$\lambda(x) \cdot f(x) = 0 \quad \Longleftrightarrow \quad x = x$$

$$\lambda(x) f(x) + x = x$$

Определяем $\psi(x) := x + \lambda(x) f(x)$. Тогда:

$$\psi(x) = x \iff f(x) = 0$$

то есть корни $f$ совпадают с неподвижными точками $\psi$.

> [!theorem] Теорема Банаха (об условии сходимости)
> Если $\psi$ — сжатие, то $\exists!\, x^*: x^* = \psi(x^*)$.
>
> Достаточное условие сжатия: $|\psi'(x)| < 1$.

**Итерационная формула:**

$$x_{n+1} = \psi(x_n)$$

$$\lim_{n \to \infty} x_n = x^*$$

> [!warning] Выбор $\lambda(x)$
> Функция $\lambda(x)$ выбирается так, чтобы обеспечить $|\psi'(x)| < 1$ в окрестности корня. Это ключевое условие сходимости.

---

## 3. Метод Ньютона (метод касательных)

**Идея:** в текущей точке $x_0$ провести касательную к графику $y = f(x)$ и взять точку пересечения касательной с осью $x$ как следующее приближение.

> [!note] График
> Касательная в точке $(x_0,\, f(x_0))$:
> $$y = f'(x_0)(x - x_0) + f(x_0)$$

Из уравнения касательной при $y = 0$:

$$0 = f'(x_0)(x - x_0) + f(x_0)$$

$$f'(x_0)(x - x_0) = -f(x_0)$$

$$(x - x_0) = -\frac{f(x_0)}{f'(x_0)}$$

$$x = x_0 - \frac{f(x_0)}{f'(x_0)}$$

> [!algorithm] Итерационная формула Ньютона
> $$\boxed{x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}}$$

**Условие Ньютона** (выбор начального приближения):

$$f(x_0) \cdot f''(x_0) > 0$$

> [!warning] Локальность
> Метод Ньютона **локален** — сходится только при достаточно близком начальном приближении к корню.

> [!summary] Сравнение методов
> - **Дихотомия:** всегда сходится, линейная скорость $O(2^{-n})$
> - **Простая итерация:** сходится при $|\psi'| < 1$, линейная скорость
> - **Ньютон:** локален, но сходимость **квадратичная** — число верных знаков удваивается на каждом шаге
