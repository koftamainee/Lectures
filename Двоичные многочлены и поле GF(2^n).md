---
title: Двоичные многочлены и поле GF(2^n)
date: 2026-03-09
tags:
  - математика
  - арифметика
  - конечные поля
  - двоичные многочлены
  - GF
aliases:
  - Поле Галуа
  - GF(2^n)
  - Умножение в конечном поле
---

## Связанные темы
[[Кольца многочленов|Кольцо многочленов]] | [[Алгоритм Карацубы]] | [[Алгоритм деления многочленов]]

---

## Конечное поле $GF(2^n)$

> [!definition] Определение: $GF(2^n)$
> $GF(2^n)$ — конечное поле из $2^n$ элементов.
>
> Элемент $f \in GF(2^n)$ представляется как многочлен:
> $$f = \sum_{j=0}^{n-1} f_j\, x^j, \quad \text{где } f_j \in \mathbb{Z}_2 = \{0, 1\}$$

> [!note] Коэффициенты из $\mathbb{Z}_2$
> $\mathbb{Z}_2$ — **поле вычетов** по модулю 2, то есть множество $\{0, 1\}$ с операциями:
> - Сложение: побитовый **XOR** ($1 + 1 = 0$, $0 + 1 = 1$)
> - Умножение: обычное AND ($1 \cdot 1 = 1$, $1 \cdot 0 = 0$)

> [!tip] Представление в памяти
> Каждый элемент $GF(2^n)$ — просто **$n$-битное слово**. Сложение — это поразрядный XOR!

---

## Умножение в $GF(2^n)$

Простое умножение многочленов над $\mathbb{Z}_2$ может дать многочлен степени $> n-1$, который выходит за пределы поля. Поэтому умножение берётся **по модулю** фиксированного многочлена $m(x)$.

> [!definition] Умножение в $GF(2^n)$
> $$f \cdot g := (f * g) \bmod m(x)$$
> где $m(x)$ — **неприводимый** многочлен степени $n$ над $\mathbb{Z}_2$ (не раскладывается на множители над $\mathbb{Z}_2$).

> [!note] Почему неприводимый?
> Неприводимость $m(x)$ гарантирует, что $\mathbb{Z}_2[x] / (m(x))$ является **полем** (а не просто кольцом). Это обеспечивает существование обратного элемента для умножения.

---

## Пример: $GF(2^8)$

**Параметры:** $n = 8$, модуль $m(x) = 0\text{x}11\text{b}_{16}$.

Переведём $0\text{x}11\text{b}$ в двоичный вид:

$$0\text{x}11\text{b} = 1\,0001\,1011_2$$

Это соответствует многочлену:

$$m(x) = x^8 + x^4 + x^3 + x + 1$$

Умножение в $GF(2^8)$:

$$f \cdot g = (f * g) \bmod (x^8 + x^4 + x^3 + x + 1)$$

---

## Практический пример

**Умножим** $57_{16}$ и $83_{16}$ в $GF(2^8)$.

Переводим в двоичный (и полиномиальный) вид:

$$57_{16} = 0101\,0111_2 \longleftrightarrow x^6 + x^4 + x^2 + x + 1$$

$$83_{16} = 1000\,0011_2 \longleftrightarrow x^7 + x + 1$$

**Перемножаем** как обычные многочлены над $\mathbb{Z}_2$ (коэффициенты по модулю 2):

$$(x^6 + x^4 + x^2 + x + 1)(x^7 + x + 1)$$

$$= x^{13} + x^{11} + x^6 + x^5 + x^4 + x^9 + x^3 + x^2 + x^8 + x^2 + x + x^7 + x + 1$$

После приведения подобных (сложение коэффициентов по mod 2, одинаковые степени аннигилируют):

<!-- ? Финальный результат после редукции нечётко написан на последней строке страницы — нужна проверка -->

$$\equiv x^7 + x^6 + 1 \pmod{x^8 + x^4 + x^3 + x + 1}$$

> [!warning] Частая ошибка
> В $GF(2^n)$ сложение коэффициентов — это **XOR**, а не обычная арифметика. Слагаемые с одинаковой степенью уничтожают друг друга: $x^k + x^k = 0$.

---

## Итог

$GF(2^n)$ — конечное поле, элементы которого суть двоичные многочлены степени $< n$. Сложение — побитовый XOR. Умножение — классическое умножение многочленов с последующей редукцией по неприводимому модулю $m(x)$. Эта структура используется, в частности, в криптографии (AES работает в $GF(2^8)$).
