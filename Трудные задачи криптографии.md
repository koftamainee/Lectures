---
title: Трудные задачи криптографии
date: 2026-04-02
tags:
  - криптография
  - информационная-безопасность
  - вычислительная-сложность
aliases:
  - Задача дискретного логарифма
  - Задача Диффи-Хеллмана
  - Факторизация
---

## Связанные темы
[[Алгоритм RSA]] | [[Функция Эйлера и теорема Эйлера]] | [[Протокол Диффи-Хеллмана]]

---

## Трудные задачи (при $N = pq$, $p, q$ — простые)

### 1. Факторизация

Разложение числа $N$ на простые множители. Лучшие известные алгоритмы — субэкспоненциальные, но не полиномиальные.

### 2. Задача RSA

Дано $c$, $e$ такие, что $\gcd(e, \varphi(N)) = 1$. Найти $m$: $m^e \equiv c \pmod{N}$.

### 3. Тест на квадратичный вычет

Определить, является ли число $A$ полным квадратом по модулю $N$:
$$x^2 \equiv A \pmod{N}$$

### 4. Извлечение квадратного корня

Дано $A$, $N$. Найти решение $x^2 \equiv A \pmod{N}$.

### 5. Дискретное логарифмирование

$G$ — абелева группа, $A \in G$, $n \in \mathbb{N}$: $A^n = B \in G$.

Дано $A$ и $B \in G$. Найти $x$:
$$A^x = B \quad \Longleftrightarrow \quad x = \log_A B$$

### 6. Задача Диффи-Хеллмана (CDH)

$A, B, C \in G$, $B = A^x$, $C = A^y$. Найти $D = A^{xy}$.

### 7. Проблема выбора Диффи-Хеллмана (DDH)

$A, B, C, D \in G$, $B = A^x$, $C = A^y$, $D = A^z$.

Необходимо узнать: $z = xy$?

---

> [!note] Иерархия сложности
> Факторизация $\Rightarrow$ задача RSA $\Rightarrow$ квадратный корень. Дискретное логарифмирование $\Rightarrow$ CDH $\Rightarrow$ DDH. Все задачи считаются вычислительно трудными — их сложность лежит в основе современной криптографии.
