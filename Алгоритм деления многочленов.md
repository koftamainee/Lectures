---
title: "Алгоритм деления многочленов"
date: 2026-03-16
tags: [математика, алгебраическая-геометрия, деление, многочлены, алгоритм]
aliases: ["Деление многочленов", "Старший член", "Алгоритм деления"]
---
## Связанные темы
[[Упорядочивание мономов]] | [[Кольцо многочленов от n переменных и идеалы]] | [[Базис Грёбнера]]

---

## Терминология: старший член, старший коэффициент

Пусть $f \in k[x_1, x_2, \ldots, x_n]$, $>$ — мономиальное упорядочивание.

> [!definition] Определение: характеристики многочлена
> 1. **Мультистепень** $f$: $\;\mathrm{multideg}(f) := \max\bigl(\alpha \in \mathbb{Z}_{\geq 0}^n \mid a_\alpha \neq 0\bigr)$
> 2. **Старший коэффициент** $f$: $\;\mathrm{lc}(f) := c_{\mathrm{multideg}(f)} \in k \setminus \{0\}$
> 3. **Старший моном** $f$: $\;\mathrm{lm}(f) := x^{\mathrm{multideg}(f)} \in k$
> 4. **Старший член** $f$: $\;\mathrm{lt}(f) := \mathrm{lc}(f) \cdot \mathrm{lm}(f)$

---

## Алгоритм деления в $k[x_1, \ldots, x_n]$

> [!algorithm] Алгоритм деления многочлена $f$ на $f_1, f_2, \ldots, f_s$
> **Вход:** $f \in k[x_1, \ldots, x_n]$, упорядоченный набор $f_1, f_2, \ldots, f_s \in k[x_1, \ldots, x_n]$
>
> **Цель:** представить $f$ в виде
> $$f = a_1 f_1 + a_2 f_2 + \cdots + a_s f_s + r,$$
> где $a_1, a_2, \ldots, a_s, r \in k[x_1, \ldots, x_n]$, и ни один моном из $r$ не делится ни на один из $\mathrm{lt}(f_i)$.
>
> **Шаги:**
> 1. Смотрим на $\mathrm{lt}(f)$
> 2. Если $\mathrm{lt}(f_i) \mid \mathrm{lt}(f)$ для некоторого $i$ — делим: вычитаем $\dfrac{\mathrm{lt}(f)}{\mathrm{lt}(f_i)} \cdot f_i$ из $f$, прибавляем $\dfrac{\mathrm{lt}(f)}{\mathrm{lt}(f_i)}$ к $a_i$
> 3. Если ни одно $\mathrm{lt}(f_i)$ не делит $\mathrm{lt}(f)$ — переносим $\mathrm{lt}(f)$ в остаток $r$
> 4. Повторяем до тех пор, пока $f \neq 0$

---

## Примеры деления

### Пример 1

$f = xy^2 + 1$, $f_1 = xy + 1$, $f_2 = y + 1$, упорядочивание $>_{\mathrm{lex}}$.

Проверяем: $f_1 >_{\mathrm{lex}} f_2$ (так как $\mathrm{lm}(f_1) = xy >_{\mathrm{lex}} y = \mathrm{lm}(f_2)$).

**Деление $f$ на $f_1$:**

$$\frac{xy^2 + 1}{xy + 1}$$

$$xy^2 + 1 \;\div\; (xy+1):$$
- $xy^2 \div xy = y$ → вычитаем $y(xy+1) = xy^2 + y$
- Остаток: $-y + 1$
- $-y \div xy$ — не делится → переносим $-y$ в $r$
- $1 \div xy$ — не делится → переносим $1$ в $r$

$$f = y \cdot (xy+1) + (-1)(y+1) + 2$$

Итог: $a_1 = y$, $a_2 = -1$, $r = 2$.

---

### Пример 2

$f = x^2y + xy^2 + y^2$, $f_1 = xy - 1$, $f_2 = y^2 - 1$, упорядочивание $>_{\mathrm{lex}}$.

Мультистепени: $f_1 \leftrightarrow (1,1)$, $f_2 \leftrightarrow (0,2)$.

**Шаг 1.** $\mathrm{lt}(f) = x^2y$. $\mathrm{lt}(f_1) = xy$ делит $x^2y$? Да: $x^2y / xy = x$.

Вычитаем $x \cdot f_1 = x(xy-1) = x^2y - x$:

$$f \leftarrow (x^2y + xy^2 + y^2) - (x^2y - x) = xy^2 + x + y^2$$

**Шаг 2.** $\mathrm{lt} = xy^2$. Делится ли на $xy$? Да: $xy^2 / xy = y$.

Вычитаем $y \cdot f_1 = y(xy-1) = xy^2 - y$:

$$f \leftarrow (xy^2 + x + y^2) - (xy^2 - y) = x + y^2 + y$$

**Шаг 3.** $\mathrm{lt} = y^2$. На $xy$ не делится. На $y^2$ делится: $y^2/y^2 = 1$.

Вычитаем $1 \cdot f_2 = y^2 - 1$:

$$f \leftarrow (x + y^2 + y) - (y^2 - 1) = x + y + 1$$

**Шаг 4.** $x + y + 1$: $x$ не делится ни на $xy$, ни на $y^2$; $y$ не делится; $1$ не делится → остаток.

$$f = (x+y)(xy-1) + (1)(y^2-1) + (x + y + 1)$$

> [!note] Важное замечание
> В $k[x_1, \ldots, x_n]$ остаток от деления **зависит от порядка делителей** $f_1, f_2, \ldots, f_s$!
> Это ключевое отличие от одномерного случая и мотивирует введение базиса Грёбнера.

