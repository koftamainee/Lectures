---
title: Метод Ньютона для систем уравнений
date: 2026-03-02
tags:
  - математика
  - арифметика
  - численные-методы
  - системы-уравнений
aliases:
  - Метод Ньютона для нелинейных систем
  - Многомерный метод Ньютона
---

## Связанные темы
[[Решение скалярных уравнений]] | [[Деление Ньютона]]

---

## Постановка задачи

Решить систему:

$$F(x) = \mathbf{0}, \quad \mathbf{0} = (0, 0, \ldots, 0) \in \mathbb{R}^n$$

где $x \in \mathbb{R}^n$, $F : M \to \mathbb{R}^n$, $M \subseteq \mathbb{R}^n$.

---

## Итерационная формула

Обобщение метода Ньютона на системы:

$$\boxed{x_{n+1} = x_n - J(x_n)^{-1} F(x_n)}$$

где $J(x_n)$ — **матрица Якоби** отображения $F$ в точке $x_n$.

> [!definition] Производная (дифференциал Фреше)
> $F'(x)$ — линейный оператор такой, что:
> $$\|F(x + h) - F(x) - F'(x)h\| = o(\|h\|)$$
>
> Матрица этого оператора — матрица Якоби:
> $$F'(x) = \left(\frac{\partial F_i}{\partial x_j}\right)_{\substack{i = 1,\ldots,n \\ j = 1,\ldots,n}}$$

> [!note] Практика
> Метод локально биективен. Начальное приближение $x_0$ можно брать любым в конечном пространстве, но не в бесконечномерном. Сходимость — $O(h^2)$ (быстрая).

---

## Теорема о квадратичной сходимости

> [!theorem] Теорема (сходимость метода Ньютона для систем)
> Пусть $(F'(x))^{-1}$ — ограниченный оператор, т.е. $\exists\, C_1, C_2 > 0$:
> $$\|(F'(x))^{-1} y\| \leq C_1 \|y\|$$
> и
> $$\|F(u_1) - F(u_2) - F'(u_2)(u_1 - u_2)\| \leq C_2 \|u_1 - u_2\|^2$$
> для $\forall y$ и $u_1, u_2$ из некоторой окрестности корня.
>
> Тогда $\forall\, x_0$ из окрестности корня:
> $$\|x_n - x^*\| = O\!\left(\|x_0 - x^*\|^{2^n}\right)$$

> [!proof]- Доказательство
> Из формулы Ньютона:
> $$F(x_n) = -F'(x^n)(x^{n+1} - x^n)$$
>
> Считаем, что $x = x^*$, т.е. $F(x) = 0$:
> $$\|F'(x^n)(x^{n+1} - x^n) - F'(x^n)(x - x^n)\| \leq C_2\|x^* - x^n\|^2$$
>
> Раскрывая:
> $$\|F'(x^n)\,x^{n+1} - F'(x^n)\,x^n - F'(x^n)\,x^* + F'(x^n)\,x^n\| \leq C_2\|x^* - x^n\|^2$$
>
> $$\|F'(x^n)(x^{n+1} - x^*)\| \leq C_2\|x^* - x^n\|^2$$
>
> Пусть $F'(x)\,y = z$ (умножение матрицы Якоби на вектор). Тогда $\|F'(x)\,y\| = \|z\|$.
>
> Из ограниченности обратного оператора:
> $$y = (F'(x))^{-1} z \Rightarrow \|y\| = \|(F'(x))^{-1} z\| \leq C_1 \|z\|$$
>
> $$\|z\| \geq \frac{\|y\|}{C_1} \Rightarrow \|F'(x)\,y\| \geq \frac{\|y\|}{C_1}$$
>
> Подставляя $y = x^{n+1} - x^*$:
> $$\|F'(x^n)(x^{n+1} - x^n)\| \geq \frac{\|x^{n+1} - x^*\|}{C_1}$$
>
> Объединяя:
> $$\|x^{n+1} - x^*\| \leq C_1 \cdot C_2 \cdot \|x^* - x^n\|^2$$
>
> $$\|x^{n+1} - x^*\| = O\!\left(\|x^n - x^*\|^2\right)$$
>
> Что и требовалось доказать. $\blacksquare$
