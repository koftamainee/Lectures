---
title: Функция Эйлера и теорема Эйлера
date: 2026-02-18
tags:
  - криптография
  - теория-чисел
  - информационная-безопасность
aliases:
  - Функция Эйлера
  - Теорема Эйлера
  - phi(n)
---

## Связанные темы
[[Основы криптографии]] | [[Алгоритм RSA]]

---

## Кольцо $\mathbb{Z}_n$

> [!definition] Кольцо вычетов $\mathbb{Z}_n$
> $$\mathbb{Z}_n := \{0, 1, \ldots, n-1\}$$
> с операциями:
> $$\forall a, b \in \mathbb{Z}_n:\quad a + b = (a + b) \bmod n, \quad a \cdot b = (a \cdot b) \bmod n$$

> [!definition] Приведённая система вычетов $\mathbb{Z}_n^*$
> $$\mathbb{Z}_n^* = \{\,l : l \in \mathbb{Z}_n,\ \gcd(l, n) = 1\,\}$$
>
> $\langle \mathbb{Z}_n^*, \cdot \rangle$ — абелева группа.

---

## Функция Эйлера

> [!definition] Функция Эйлера
> $$\varphi(n) := \sum_{\substack{1 \leq k < n \\ \gcd(k,\,n) = 1}} 1 \quad = \#\mathbb{Z}_n^*$$

**Формулы вычисления:**

$$\varphi(p) = p - 1 \quad (p \text{ — простое})$$

$$\varphi(p^\alpha) = p^\alpha - p^{\alpha-1}$$

$$\varphi(n \cdot m) = \varphi(n)\,\varphi(m) \quad \text{если } \gcd(n, m) = 1$$

---

## Теорема Эйлера

> [!theorem] Теорема Эйлера
> Пусть $a, n \in \mathbb{N}$, $\gcd(a, n) = 1$. Тогда:
> $$a^{\varphi(n)} \equiv 1 \pmod{n}$$

> [!proof]- Доказательство
> Рассмотрим отображение $f : \mathbb{Z}_n^* \to \mathbb{Z}_n^*$, $f(x) = a \cdot x \bmod n$.
>
> Пусть $x_1, x_2, \ldots, x_{\varphi(n)}$ — все элементы $\mathbb{Z}_n^*$.
>
> **Шаг 1.** Нет повторений $\Rightarrow$ $f$ — перестановка (биекция).
>
> Допустим, есть повторение: $\exists\, i \neq j : a x_i \equiv a x_j \pmod{n}$.
> Тогда $a(x_i - x_j) \equiv 0 \pmod{n}$.
> Но $a$ и $n$ взаимно просты $\Rightarrow$ $x_i \equiv x_j \pmod{n}$ — противоречие.
>
> **Шаг 2.** Из того, что $f$ — перестановка элементов:
> $$ax_1, ax_2, \ldots, ax_{\varphi(n)} \text{ — те же элементы в другом порядке}$$
>
> $$\prod_{i=1}^{\varphi(n)} ax_i \equiv \prod_{i=1}^{\varphi(n)} x_i \pmod{n}$$
>
> $$a^{\varphi(n)} \prod_{i=1}^{\varphi(n)} x_i \equiv \prod_{i=1}^{\varphi(n)} x_i \pmod{n}$$
>
> Так как $\prod x_i$ взаимно просто с $n$ (все $x_i \in \mathbb{Z}_n^*$), сокращаем:
> $$a^{\varphi(n)} \equiv 1 \pmod{n} \quad \blacksquare$$
