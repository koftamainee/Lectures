---
title: Дискретное преобразование Фурье
date: 2026-03-16
tags:
  - математика
  - арифметика
  - ДПФ
  - свёртка
  - DFT
aliases:
  - ДПФ
  - DFT
---

## Связанные темы
[[Корни из единицы и примитивный корень]] | [[Матрица Вандермонда и обратное ДПФ]] | [[Быстрое преобразование Фурье (FFT)]] | [[Кольца многочленов]]

---

## Постановка

Пусть $f \in R[x]$, $\deg f < n$. Тогда $f$ однозначно задаётся вектором коэффициентов:

$$f \longleftrightarrow (f_0, f_1, f_2, \ldots, f_{n-1}) \in R^n$$

---

## Определение ДПФ

> [!definition] Определение: Дискретное преобразование Фурье
> Пусть $\omega$ — примитивный корень $n$-й степени из единицы в $R$. **Дискретным преобразованием Фурье** называется отображение:
> $$\mathrm{DFT}_\omega : R^n \to R^n, \qquad f \mapsto \bigl(f(1),\, f(\omega),\, f(\omega^2),\, \ldots,\, f(\omega^{n-1})\bigr)$$
> то есть вектор значений многочлена $f$ во всех степенях $\omega$.

> [!note] Интерпретация
> ДПФ переводит многочлен из **представления коэффициентами** в **представление значениями** в точках $1, \omega, \omega^2, \ldots, \omega^{n-1}$.

---

## Свёртка многочленов

> [!definition] Определение: Свёртка
> **Свёрткой** двух многочленов $f$ и $g$ (степени $< n$) называется многочлен $h = f \star g$ степени $< n$ с коэффициентами:
> $$h_k = \sum_{\substack{j + i = k \pmod{n}}} f_i \cdot g_j$$
> то есть умножение ведётся по **циклическому** правилу с периодом $n$.

---

## Лемма о свёртке и умножении многочленов

> [!theorem] Лемма
> Пусть $f, g \in R[x]$, $\deg f < n$, $\deg g < n$. Тогда:
> $$f \star g = f \cdot g \pmod{x^n - 1}$$

> [!proof]- Доказательство
>
> Запишем произведение напрямую:
> $$f \cdot g = \left(\sum_{j=0}^{n-1} f_j x^j\right)\!\left(\sum_{\ell=0}^{n-1} g_\ell\, x^\ell\right) = \sum_{k=0}^{2n-2} \left(\sum_{j+\ell=k} f_j g_\ell\right) x^k$$
>
> Нам нужно взять остаток по модулю $x^n - 1$. Используем тот факт, что для $k = q \cdot n + r$ (где $r = k \bmod n$):
> $$x^k \bmod (x^n - 1) = x^{qn + r} \bmod (x^n - 1) = (x^n)^q \cdot x^r \bmod (x^n-1)$$
>
> Так как $x^n \equiv 1 \pmod{x^n - 1}$, то $(x^n)^q \equiv 1$. Значит:
> $$x^k \bmod (x^n - 1) = x^{k \bmod n}$$
>
> Следовательно:
> $$f \cdot g \bmod (x^n - 1) = \sum_{k=0}^{n-1}\left(\sum_{\substack{i+j = k \pmod{n}}} f_i g_j\right) x^k = f \star g \qquad \square$$

---

## Ключевая лемма: свёртка через ДПФ

> [!theorem] Лемма о свёртке через ДПФ
> Пусть $f, g \in R[x]$, $\deg f < n$, $\deg g < n$. Тогда:
> $$\mathrm{DFT}_\omega(f \star g) = \mathrm{DFT}_\omega(f) \cdot \mathrm{DFT}_\omega(g)$$
> где справа стоит **покоординатное произведение** векторов.
>
> Эквивалентно:
> $$f \star g = \mathrm{DFT}_\omega^{-1}\!\bigl(\mathrm{DFT}_\omega(f) \cdot \mathrm{DFT}_\omega(g)\bigr)$$

> [!proof]- Доказательство
>
> По лемме о свёртке, $f \star g = f \cdot g + q(x)(x^n - 1)$ для некоторого $q \in R[x]$.
>
> Вычислим $k$-ю компоненту $\mathrm{DFT}_\omega(f \star g)$, то есть значение в $\omega^k$:
> $$(f \star g)(\omega^k) = f(\omega^k)\cdot g(\omega^k) + q(\omega^k)\cdot\underbrace{((\omega^k)^n - 1)}_{=\,\omega^{kn}-1\,=\,1-1\,=\,0}$$
>
> Значит: $(f \star g)(\omega^k) = f(\omega^k) \cdot g(\omega^k)$
>
> Это выполнено для всех $k = 0, 1, \ldots, n-1$, что и означает покоординатное равенство. $\square$

> [!tip] Важный частный случай
> Если $\deg f + \deg g < n$, то $f \star g = f \cdot g$ (без редукции по модулю), и лемма даёт способ вычислить **обычное произведение** многочленов через ДПФ.

---

## Итог

ДПФ — это оценка многочлена во всех корнях из единицы. Ключевое свойство: **свёртка в пространстве коэффициентов = покоординатное умножение в пространстве значений**. Это позволяет умножать многочлены за $O(n \log n)$ с помощью [[Быстрое преобразование Фурье (FFT)|FFT]].

