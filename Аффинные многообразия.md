---
title: Аффинные многообразия
date: 2026-03-16
tags:
  - математика
  - аффинные-многообразия
  - идеалы
  - арифметика
aliases:
  - Аффинное многообразие
  - Идеал многообразия
---
## Связанные темы
[[Кольцо многочленов от n переменных и идеалы]] | [[Системы полиномиальных уравнений]] | [[Мономиальные идеалы]]

---

## Определение аффинного многообразия

> [!definition] Определение: Аффинное многообразие $V(f_1, \ldots, f_s)$
> Пусть $k$ — поле, $f_1, f_2, \ldots, f_s \in k[x_1, x_2, \ldots, x_n]$. Полагаем:
> $$V(f_1, f_2, \ldots, f_s) := \left\{ (a_1, a_2, \ldots, a_n) \in k^n \;\Big|\; f_j(a_1, a_2, \ldots, a_n) = 0 \;\; \forall j = 1, 2, \ldots, s \right\}$$
> Множество $V(f_1, f_2, \ldots, f_s)$ называется **аффинным многообразием**.

---

## Операции над аффинными многообразиями

> [!theorem] Лемма: замкнутость относительно объединения и пересечения
> Пусть $V, W \subseteq k^n$ — аффинные многообразия. Тогда их **объединение** $V \cup W$ и **пересечение** $V \cap W$ тоже являются аффинными многообразиями.

---

## Идеал многообразия

> [!definition] Определение: $I(V)$
> Пусть множество $V \subseteq k^n$ — аффинное многообразие. Полагаем:
> $$I(V) = \left\{ f \in k[x_1, \ldots, x_n] \;\Big|\; f(A) = 0 \;\; \forall A \in V \right\}$$
> то есть $I(V)$ — **множество всех многочленов, обращающихся в 0 на $V$**.

> [!theorem] Лемма: $I(V)$ — идеал
> Пусть $V \subseteq k^n$ — аффинное многообразие. Тогда $I(V)$ — идеал, который называется **идеалом многообразия $V$**.

> [!proof]- Доказательство
> Проверяем три условия идеала:
> 1. $0 \in I(V)$ — нулевой многочлен обращается в 0 везде.
> 2. $\forall f, g \in I(V)$: $(f+g)(A) = f(A) + g(A) = 0 + 0 = 0$, значит $f + g \in I(V)$.
> 3. $\forall h \in k[x_1,\ldots,x_n],\; f \in I(V)$: $(fh)(A) = f(A) \cdot h(A) = 0 \cdot h(A) = 0$, значит $fh \in I(V)$.

---

## Замечания

> [!note] Частные случаи
> - Если $V = k^n$, то $I(V) = \{0\}$ (только нулевой многочлен обращается в 0 всюду).
> - Если $V = \{(0, 0)\}$, то $I(V) = \langle x, y \rangle$.
>   **Доказательство:** многочлен $f = \sum_{i,j} a_{ij} x^i y^j$ обращается в 0 в $(0,0)$ тогда и только тогда, когда $a_{0,0} = 0$, значит $f = x(\ldots) + y(\ldots) \in \langle x, y \rangle$.

---

## Соответствие между идеалами и многообразиями

> [!theorem] Лемма
> Пусть $f_1, f_2, \ldots, f_s \in k[x_1, x_2, \ldots, x_n]$. Тогда:
> $$\langle f_1, f_2, \ldots, f_s \rangle \subseteq I\bigl(V(f_1, f_2, \ldots, f_s)\bigr)$$

> [!proof]- Доказательство
> $\forall f \in \langle f_1, \ldots, f_s \rangle$: $f = \sum h_j f_j$, где $h_j \in k[\ldots]$.
> $\forall A \in V(f_1, \ldots, f_s)$: $f(A) = \sum h_j(A) f_j(A) = 0$, значит $f \in I(V(f_1,\ldots,f_s))$.

> [!note] Замечание о включении $\langle f \rangle \Rightarrow V(f) \Rightarrow I(V(f))$
> Цепочка $k[x_1,\ldots,x_n] \xrightarrow{V} k^n \xrightarrow{I} k[x_1,\ldots,x_n]$ в общем случае **не биективна**.
>
> Контрпример: $(0,0) \in V(x^2, y^2)$, но $I(V(x^2, y^2)) = \langle x, y \rangle \supsetneq \langle x^2, y^2 \rangle$.

---

## Монотонность $I(V)$

> [!note] Замечание: монотонность
> Пусть $V, W$ — аффинные многообразия в $k^n$, $V, W \subseteq k^n$. Тогда:
> - $V \subseteq W \implies I(V) \supseteq I(W)$
> - $V = W \implies I(V) = I(W)$
