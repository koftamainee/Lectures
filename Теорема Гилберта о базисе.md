---
title: Теорема Гилберта о базисе
date: 2026-04-06
tags:
  - математика
  - арифметика
  - идеалы
  - базис-Грёбнера
aliases:
  - Hilbert basis theorem
  - теорема о базисе Гилберта
---

## Связанные темы
[[Кольцо многочленов от n переменных и идеалы]] | [[Базис Грёбнера]] | [[Мономиальные идеалы]] | [[Аффинные многообразия]]

---

## Теорема Гилберта о базисе

> [!theorem] Теорема Гилберта о базисе
> Каждый идеал $I \subseteq k[x_1, x_2, \ldots, x_n]$ конечно порождён, т.е.
> $$\forall I \subseteq k[x_1, x_2, \ldots, x_n] \quad \exists f_1, f_2, \ldots, f_s \in k[x_1, \ldots, x_n] : \quad I = \langle f_1, \ldots, f_s \rangle$$

> [!proof]- Доказательство
> Если $I = \{0\}$, то $I = \langle 0 \rangle$, что конечно порождён.
>
> Пусть $I \neq \{0\}$. Из утверждения о существовании базиса Грёбнера (см. [[Базис Грёбнера]]) следует, что существуют $g_1, \ldots, g_s \in I$ такие, что
> $$\langle LT(I) \rangle = \langle \mathrm{lt}(g_1), \ldots, \mathrm{lt}(g_s) \rangle$$
>
> Покажем, что $I = \langle g_1, \ldots, g_s \rangle$.
>
> Пусть $f \in I$. Разделим $f$ на $g_1, g_2, \ldots, g_s$:
> $$f = \sum_{j=1}^{s} a_j g_j + r$$
> где ни один член остатка $r$ не делится ни на один $\mathrm{lt}(g_i)$.
>
> Так как $r = f - \sum a_j g_j \in I$, то $\mathrm{lt}(r) \in \langle LT(I) \rangle = \langle \mathrm{lt}(g_1), \ldots, \mathrm{lt}(g_s) \rangle$.
>
> Значит, $\mathrm{lt}(r)$ должен делиться хотя бы на один $\mathrm{lt}(g_i)$ — противоречие с определением остатка.
>
> Следовательно $r = 0$, и $f = \sum a_j g_j \in \langle g_1, \ldots, g_s \rangle$.
>
> Таким образом $I \subseteq \langle g_1, \ldots, g_s \rangle$, а поскольку все $g_i \in I$, то $\langle g_1, \ldots, g_s \rangle \subseteq I$.
>
> Итого: $I = \langle g_1, \ldots, g_s \rangle$.

---

## Теорема об обрыве возрастающей последовательности

> [!definition] Определение: возрастающая цепь идеалов
> **Возрастающей цепью идеалов** называется последовательность идеалов
> $$I_1 \subseteq I_2 \subseteq I_3 \subseteq \ldots$$

> [!theorem] Теорема об обрыве возрастающей последовательности
> Пусть $I_1 \subseteq I_2 \subseteq \ldots$ — возрастающая цепь идеалов в кольце многочленов $k[x_1, \ldots, x_n]$. Тогда
> $$\exists N \in \mathbb{N} : \quad I_N = I_{N+1} = I_{N+2} = \ldots$$

> [!proof]- Доказательство
> Рассмотрим $I_1 \subseteq I_2 \subseteq \ldots$ — возрастающую цепь идеалов.
>
> Пусть $I = \bigcup_{j=1}^{\infty} I_j$. Покажем, что $I$ — идеал (т.к. $I_j \subseteq I$).
>
> Если $f, g \in I$, то $f \in I_i$, $g \in I_j$ для некоторых $i, j$. При $i \leq j$: $f \in I_j$, значит $f + g \in I_j \subseteq I$.
>
> Если $f \in I$, $p \in k[x_1, \ldots, x_n]$: $f \in I_i \Rightarrow pf \in I_i \subseteq I$.
>
> Значит $I$ — идеал. По теореме Гилберта $I$ конечно порождён:
> $$I = \langle f_1, \ldots, f_s \rangle$$
> где $f_i \in I_{j_i}$ для некоторых $j_i$.
>
> Пусть $N = \max(j_i)$. Тогда $f_i \in I_N$ для всех $i$, поэтому
> $$I = \langle f_1, \ldots, f_s \rangle \subseteq I_N \subseteq I_{N+1} \subseteq \ldots \subseteq I$$
>
> Следовательно $I_N = I_{N+1} = I_{N+2} = \ldots = I$.

---

## Следствие: аффинные многообразия

> [!note] Следствие для аффинных многообразий
> Пусть $V(I)$ — алгебраическое многообразие. Если $I = \langle f_1, \ldots, f_s \rangle$, то
> $$V(I) = V(f_1, \ldots, f_s)$$
>
> **Доказательство.** $I$ конечно порождён по теореме Гилберта. Далее, если $p(a) = 0$ для всех $f \in I$, то в частности $f_i(a) = 0$ для $i = 1, \ldots, s$, т.е. $V(I) \subseteq V(f_1, \ldots, f_s)$.
>
> Обратно, пусть $a \in V(f_1, \ldots, f_s)$ и $f \in I$: тогда $f = \sum h_i f_i$, поэтому $f(a) = \sum h_i(a) f_i(a) = 0$. Значит $V(f_1, \ldots, f_s) \subseteq V(I)$.

---

## Итог

> [!summary] Резюме
> Теорема Гилберта — фундаментальный результат коммутативной алгебры: кольцо $k[x_1, \ldots, x_n]$ является **нётеровым**, т.е. в нём любая возрастающая цепь идеалов обрывается. Это гарантирует, что базис Грёбнера существует для любого ненулевого идеала.
