---
title: Матрица Вандермонда и обратное ДПФ
date: 2026-03-16
tags:
  - математика
  - арифметика
  - ДПФ
  - матрица-Вандермонда
  - обратное-ДПФ
aliases:
  - Обратное ДПФ
  - Матрица Вандермонда
---

## Связанные темы
[[Корни из единицы и примитивный корень]] | [[Дискретное преобразование Фурье]] | [[Быстрое преобразование Фурье (FFT)]]

---

## Матричный взгляд на ДПФ

Пусть $n = 2^k$, $\deg f < n$, $\deg g < n$. Разобьём $f$ и $g$ на половины:

$$f(x) = F_1 \cdot x^{n/2} + F_0, \qquad g(x) = G_1 \cdot x^{n/2} + G_0$$

ДПФ можно записать как умножение на матрицу. Определим **матрицу Вандермонда**:

$$V_\omega = \begin{pmatrix} 1 & 1 & 1 & \cdots & 1 \\ 1 & \omega & \omega^2 & \cdots & \omega^{n-1} \\ 1 & \omega^2 & \omega^4 & \cdots & \omega^{2(n-1)} \\ \vdots & \vdots & \vdots & \ddots & \vdots \\ 1 & \omega^{n-1} & \omega^{2(n-1)} & \cdots & \omega^{(n-1)^2} \end{pmatrix}$$

> [!note] Симметрия
> $V_\omega$ является **симметричной** матрицей: $(V_\omega)_{ij} = \omega^{ij}$.

Тогда $\mathrm{DFT}_\omega(f) = V_\omega \cdot (f_0, f_1, \ldots, f_{n-1})^\top$.

---

## Обратимость $V_\omega$

> [!theorem] Обратимость матрицы Вандермонда
> $$\det V_\omega = \prod_{\substack{i,j \\ i \neq j}} (\omega^i - \omega^j) \neq 0$$
>
> Следовательно, $V_\omega$ **обратима**, и ДПФ — биекция.

---

## Лемма: формула для обратного ДПФ

> [!theorem] Лемма: $\mathrm{DFT}_\omega^{-1} = \mathrm{DFT}_{\omega^{-1}}$
> $$V_\omega \cdot V_{\omega^{-1}} = n \cdot E$$
> где $E$ — единичная матрица. Следовательно:
> $$\mathrm{DFT}_\omega^{-1} = \frac{1}{n}\,\mathrm{DFT}_{\omega^{-1}}$$

> [!proof]- Доказательство
>
> Вычислим $(i,j)$-й элемент матрицы $V_\omega \cdot V_{\omega^{-1}}$:
>
> $$(V_\omega \cdot V_{\omega^{-1}})_{ij} = \sum_{k=0}^{n-1} (V_\omega)_{ik} \cdot (V_{\omega^{-1}})_{kj} = \sum_{k=0}^{n-1} \omega^{ik} \cdot \omega^{-kj} = \sum_{k=0}^{n-1} \omega^{(i-j)k}$$
>
> По **лемме о примитивном корне** ([[Корни из единицы и примитивный корень]]):
>
> $$\sum_{k=0}^{n-1} \omega^{(i-j)k} = \begin{cases} 0, & i \neq j \\ n, & i = j \end{cases}$$
>
> Значит, $V_\omega \cdot V_{\omega^{-1}} = n \cdot E$, откуда $V_\omega^{-1} = \frac{1}{n} V_{\omega^{-1}}$. $\square$

---

## Разложение значений ДПФ

При $n = 2^k$ и разбиении $f(x) = F_0 + x^{n/2} F_1$:

**Чётные точки** ($\omega^{2i}$):

$$f(\omega^{2i}) = F_0(\omega^{2i}) + F_1(\omega^{2i}) \cdot \underbrace{(\omega^{in} + 1)}_{= 0 \text{ т.к. } \omega^n=1} = F_0(\omega^{2i}) + F_1(\omega^{2i})$$

**Нечётные точки** ($\omega^{2i+1}$):

$$f(\omega^{2i+1}) = F_0(\omega^{2i+1}) + F_1(\omega^{2i+1}) + \underbrace{(\omega^{in+n/2} + 1)}_{=0}\cdot F_1(\omega^{2i+1}) = F_0(\omega^{2i+1}) + F_1(\omega^{2i+1}) \cdot \omega^{2i+1}$$


Вводим вспомогательные многочлены:

$$F_0^*(x) := F_0(\omega x), \qquad F_1^*(x) := F_1(\omega x)$$

Тогда: $F_0^*(\omega^{2i}) = F_0(\omega^{2i+1})$ и $F_1^*(\omega^{2i}) = F_1(\omega^{2i+1})$.

> [!tip] Смысл
> Вычисление ДПФ в нечётных точках сводится к ДПФ сдвинутых многочленов $F_0^*, F_1^*$ — в чётных точках. Это основа рекурсии FFT.

---

## Итог

ДПФ — это умножение на матрицу Вандермонда $V_\omega$. Матрица обратима, и обратное ДПФ выражается через ДПФ с заменой $\omega \mapsto \omega^{-1}$: $\mathrm{DFT}_\omega^{-1} = \frac{1}{n}\mathrm{DFT}_{\omega^{-1}}$. Разложение на чётные/нечётные точки позволяет построить [[Быстрое преобразование Фурье (FFT)|рекурсивный алгоритм FFT]].

